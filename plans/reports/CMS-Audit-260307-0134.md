# CMS Hardcoded Data Audit Report

**Date:** 2026-03-07  
**Project:** La Mer Collection Website  
**Scope:** All page files & components for CMS integration readiness

---

## Summary

- **Pages Analyzed:** 7 (home, collection, experience, story, vinh-hy, gallery, lien-he)
- **Components Analyzed:** cta-banner.tsx, constants.ts
- **Hardcoded Data Found:** Yes, significant amounts across multiple pages
- **Ready for CMS:** Partial (hero sections done, content sections need work)

---

## Page-by-Page Audit

### 1. **src/app/(site)/page.tsx** (Home Page)

**CMS Integration Status:** ~60% complete

**Hardcoded Data (NOT using CMS):**

1. **Hero section hardcoded text** (line 72)
   - `"Vĩnh Hy Bay — Ninh Thuận"` - location text is hardcoded
   - Should be: CMS field or fallback

2. **Scroll indicator text** (line 90)
   - `"Khám phá"` - hardcoded in hero
   - Should be: CMS or reusable config

3. **Collection cards data** (lines 158-242)
   - Entire card array is hardcoded with 3 fixed items:
     - "Heritage Villa" / "Villa Di Sản"
     - "Mediterranean Villa" / "Villa Địa Trung Hải"
     - "Ocean View Room" / "Phòng Khách Sạn"
   - Card images: Uses `collection?.images?.[idx]` but has NO fallback if images missing
   - **Issue:** If CMS has no images, cards show only gradients (should fail gracefully)

4. **Collection gradient backgrounds** (lines 163, 170, 177)
   - Hardcoded color gradients for each card
   - Should be: Part of CMS section data OR use category-based mapping

5. **Story intro default text** (line 121)
   - Default fallback is hardcoded English/Vietnamese mix
   - Already has CMS fallback pattern ✓

6. **Experience image fallback** (line 269)
   - Gradient-only fallback
   - Should: Accept CMS image properly

7. **All button links** are hardcoded
   - Links work but CMS could manage link targets

**CMS Fields Working:**
- ✓ Hero title/subtitle (`cms?.heroTitle`, `cms?.heroSubtitle`)
- ✓ Hero background image (`cms?.heroBackground?.asset?.url`)
- ✓ Intro section text (`intro?.body`)
- ✓ Collection section header (`collection?.subtitle/title`)
- ✓ Experience section data (`experience?.*`)
- ✓ Location section data (`location?.*`)

**To Do for CMS Migration:**
- [ ] Move collection cards from hardcoded array to CMS section data
- [ ] Add CMS field for hero location text ("Vĩnh Hy Bay — Ninh Thuận")
- [ ] Add CMS field for scroll indicator text
- [ ] Store card gradients in CMS or category-based config
- [ ] Add proper image fallback handling

---

### 2. **src/app/(site)/collection/page.tsx** (Collection Page)

**CMS Integration Status:** ~90% complete

**Hardcoded Data:**

1. **No hardcoded page text** - All uses CMS or fallback ✓

2. **SanityProperty interface** (lines 25-36)
   - Structure depends on CMS schema
   - Should verify schema matches type definitions

**CMS Fields Working:**
- ✓ Hero subtitle/title (`cms?.heroSubtitle/Title`)
- ✓ Hero background image
- ✓ All properties fetched from Sanity via `allPropertiesQuery`

---

### 3. **src/app/(site)/collection/collection-client.tsx** (Collection Client)

**CMS Integration Status:** ~80% complete

**Hardcoded Data:**

1. **CATEGORY_LABELS mapping** (lines 12-17)
   ```tsx
   const CATEGORY_LABELS: Record<string, string> = {
     heritage: "Villa Di Sản",
     mediterranean: "Villa Địa Trung Hải",
     vietnamese: "Villa Việt Nam",
     hotel: "Phòng Khách Sạn",
   };
   ```
   - These labels are hardcoded in component
   - **Should be:** CMS taxonomy or `src/lib/constants.ts`

2. **CATEGORY_GRADIENTS mapping** (lines 19-24)
   - Gradient color scheme hardcoded per category
   - **Should be:** Part of CMS category document or constants

3. **Collection filter options**
   - Uses `COLLECTION_FILTERS` from constants (external) ✓

4. **CTA Banner text** (lines 124-129)
   - Hardcoded button label: "Liên Hệ Để Đặt Phòng"
   - Hardcoded text content
   - **Should be:** CMS section or component props

**CMS Fields Working:**
- ✓ Hero data passed from parent
- ✓ Properties data from Sanity (maps to UI)
- ✓ Fallback to hardcoded PROPERTIES array if CMS empty

---

### 4. **src/app/(site)/experience/page.tsx** (Experience Page)

**CMS Integration Status:** ~40% complete

**Hardcoded Data (SIGNIFICANT):**

1. **ACTIVITIES array** (lines 15-42)
   - 4 hardcoded activities with SVG icon paths:
     - Chèo Kayak
     - Lặn Ngắm San Hô
     - Câu Cá
     - Du Thuyền Hoàng Hôn
   - **Issue:** Icon paths are hardcoded SVG data
   - Should be: CMS collection or icon library reference

2. **Activity gradient backgrounds** (lines 19-40)
   - Hardcoded colors per activity
   - No category system

3. **DINING_GALLERY_BG array** (lines 44-50)
   - 5 hardcoded gradient backgrounds for dining images
   - Used as fallback if `dining?.images` is empty
   - **Issue:** Shows only gradients if no images in CMS

4. **Button link** (line 167)
   - "Đặt Lịch Spa" button href="#" is hardcoded
   - Should: CMS field or routing config

**CMS Fields Working:**
- ✓ Hero title/subtitle
- ✓ Hero background image
- ✓ Dining section header & body
- ✓ Dining gallery images (fallback to gradients)
- ✓ Spa section data
- ✓ Activities section header (subtitle/title only)

**To Do:**
- [ ] Move ACTIVITIES array to CMS (document with icons/descriptions/gradients)
- [ ] Store activity icons in CMS (SVG or icon ID)
- [ ] Remove hardcoded activity SVG paths
- [ ] Move dining gallery gradients to CMS section
- [ ] Add CMS field for "Đặt Lịch Spa" button link

---

### 5. **src/app/(site)/story/page.tsx** (Story Page)

**CMS Integration Status:** ~45% complete

**Hardcoded Data (SIGNIFICANT):**

1. **COMMUNITY_ITEMS array** (lines 13-34)
   - 3 hardcoded community sections:
     - Ngư Dân
     - Ẩm Thực
     - Văn Hóa
   - **Issue:** Icon paths hardcoded in code
   - Should be: CMS collection

2. **Community icon SVG paths** (lines 18, 24-25, 31-32)
   - Hardcoded SVG paths for each icon
   - No icon library reference
   - Should: Use CMS icon ID or icon system

3. **Community item descriptions** (lines 16-17, 22-23, 29)
   - All text is hardcoded
   - Should be: CMS fields

4. **Origin section image** (lines 93-99)
   - No image used (gradient only)
   - Should: Accept CMS image

5. **Philosophy section blockquote** (lines 235-237)
   - Default quote is hardcoded
   - Already has CMS fallback pattern ✓
   - But: Hard-interpolated quote marks, should use config

**CMS Fields Working:**
- ✓ Hero title/subtitle/image
- ✓ Origin section header (subtitle/title)
- ✓ Origin section paragraph text (hardcoded, not CMS)
- ✓ Heritage section header & body
- ✓ Community section header (subtitle/title only)
- ✓ Philosophy section body with fallback

**To Do:**
- [ ] Move COMMUNITY_ITEMS to CMS document
- [ ] Store community icons in CMS
- [ ] Remove hardcoded SVG paths
- [ ] Add CMS field for origin section body text
- [ ] Add CMS field for origin image
- [ ] Refactor philosophy blockquote quote marks

---

### 6. **src/app/(site)/vinh-hy/page.tsx** (Vĩnh Hy Page)

**CMS Integration Status:** ~50% complete

**Hardcoded Data (SIGNIFICANT):**

1. **HIGHLIGHTS array** (lines 13-34)
   - 4 hardcoded highlight cards:
     - Biển Xanh
     - Rạn San Hô
     - Làng Chài
     - Núi Rừng
   - All text & gradients hardcoded
   - Should be: CMS collection

2. **HIGHLIGHTS gradient backgrounds** (lines 17, 22, 27, 32)
   - Hardcoded per highlight
   - No image support

3. **TRANSPORT array** (lines 36-52)
   - 3 hardcoded transport options:
     - Sân Bay Cam Ranh
     - TP. Phan Rang
     - Đón Tận Nơi
   - All info hardcoded
   - Should be: CMS array

4. **Intro section image** (lines 93-99)
   - Gradient only, no CMS image fallback

**CMS Fields Working:**
- ✓ Hero title/subtitle/image
- ✓ Intro section title/body
- ✓ Highlights section header (subtitle/title only)
- ✓ Transport section header (subtitle/title only)
- ✓ Cuisine section header & body

**To Do:**
- [ ] Move HIGHLIGHTS to CMS document (with descriptions/gradients)
- [ ] Move TRANSPORT to CMS document
- [ ] Add CMS image fields for highlight cards
- [ ] Add CMS image field for intro section
- [ ] Remove hardcoded arrays from code

---

### 7. **src/app/(site)/gallery/page.tsx** (Gallery Page)

**CMS Integration Status:** ~95% complete

**Hardcoded Data:**

1. **No hardcoded gallery items** - All use constants ✓
2. **No hardcoded page text** ✓

**Note:** Gallery content entirely in constants.ts (see below)

---

### 8. **src/app/(site)/gallery/gallery-client.tsx** (Gallery Client)

**CMS Integration Status:** ~90% complete

**Hardcoded Data:**

1. **GALLERY_FILTERS usage** (line 7)
   - Hardcoded but imported from constants (acceptable)

2. **GALLERY_ITEMS usage** (line 7)
   - All gallery items hardcoded in constants.ts
   - **Issue:** Should be in CMS for editor management

**Gallery currently uses constants for all items** - see constants audit below.

---

### 9. **src/app/(site)/lien-he/page.tsx** (Contact Page)

**CMS Integration Status:** ~95% complete

**Hardcoded Data:**

1. **No hardcoded page text** - All CMS ✓

---

### 10. **src/app/(site)/lien-he/contact-client.tsx** (Contact Client)

**CMS Integration Status:** ~70% complete

**Hardcoded Data:**

1. **Form field labels** (lines 51-56, 61-62, etc.)
   - Labels like "Đặt Phòng", "Họ tên", "Email" are hardcoded
   - Technically acceptable (UI labels) but could be CMS

2. **SITE constant usage** (lines 6, 221-254)
   - Uses `SITE.phone`, `SITE.email`, `SITE.address`, `SITE.hours`
   - These are in constants.ts (not CMS)
   - Should be: CMS site settings document

3. **Form submission messages** (lines 77-86)
   - "Cảm ơn bạn!" / "Chúng tôi sẽ liên hệ lại sớm nhất."
   - Hardcoded success message
   - Should be: CMS content or config

4. **Map placeholder text** (lines 280-290)
   - "Vĩnh Hy Bay" and "Ninh Thuận, Việt Nam" hardcoded
   - Should be: CMS field

**CMS Fields Working:**
- ✓ Hero title/subtitle/image

**To Do:**
- [ ] Move SITE config to CMS (phone, email, address, hours)
- [ ] Add CMS field for map placeholder text
- [ ] Add CMS field for form success message
- [ ] Optionally: Add CMS for form field labels (if multilingual needed)

---

### 11. **src/components/ui/cta-banner.tsx** (CTA Banner)

**CMS Integration Status:** 100% (Component accepts props)

**Hardcoded Data:**

**None** - This is a reusable component that accepts all text via props ✓

**Usage:** Called from page components with hardcoded props:
- Collection page: "Liên Hệ Để Đặt Phòng" (hardcoded in component call)
- Experience page: "Bắt Đầu Hành Trình" (hardcoded in component call)

**Note:** Hardcoding is in parent components calling this, not in the banner itself.

---

### 12. **src/lib/constants.ts** (Constants File)

**CMS Integration Status:** 20% (mostly hardcoded)

**Hardcoded Data (EXTENSIVE):**

1. **SITE config** (lines 1-13)
   - Phone, email, address, hours, name, tagline, URL
   - **Should be:** CMS site settings document
   - Currently in constants but accessed from contact page

2. **NAV_LINKS** (lines 15-23)
   - Navigation menu items (7 pages)
   - Acceptable in constants (structural, changes rarely)
   - Could be CMS if dynamic nav needed

3. **FOOTER_LINKS** (lines 25-32)
   - Footer menu items (6 pages)
   - Acceptable in constants
   - Could be CMS if dynamic footer needed

4. **PROPERTIES array** (lines 40-116)
   - 8 hardcoded property cards with:
     - Title, category, description, guests, area, gradient, image URL
   - **Used as:** Fallback when CMS has no properties
   - **Issue:** Unsplash URLs are placeholders, should remove
   - **Should be:** CMS only (remove fallback once properties in CMS)

5. **COLLECTION_FILTERS** (lines 118-123)
   - Category filter options
   - Acceptable in constants (filter structure)
   - Could be CMS for dynamic categories

6. **GALLERY_FILTERS** (lines 125-131)
   - Category filters for gallery (5 categories)
   - Acceptable in constants

7. **GALLERY_ITEMS** (lines 133-197)
   - 12 hardcoded gallery images with:
     - Title, category, gradient, variant (tall/wide)
   - **Issue:** NO actual images (only gradients + titles)
   - **Should be:** Migrated to CMS with actual image URLs
   - Currently: Placeholder-only gallery

8. **ROOM_TYPE_OPTIONS** (lines 199-204)
   - Room type select options (4 types)
   - Acceptable in constants

**Priority CMS Migration:**
- **HIGH:** PROPERTIES array (replace with CMS)
- **HIGH:** GALLERY_ITEMS array (add actual images to CMS)
- **HIGH:** SITE config (move to CMS settings)
- **MEDIUM:** COLLECTION_FILTERS (optionally CMS)
- **LOW:** NAV_LINKS, FOOTER_LINKS (keep in constants if static)
- **LOW:** ROOM_TYPE_OPTIONS (keep in constants)

---

## Summary by Page

| Page | CMS Ready | Hardcoded Items | Priority |
|------|-----------|-----------------|----------|
| Home | 60% | Hero location text, collection cards | HIGH |
| Collection | 90% | Category labels, gradients | MEDIUM |
| Experience | 40% | ACTIVITIES array, icon SVGs | **CRITICAL** |
| Story | 45% | COMMUNITY_ITEMS, icon SVGs | **CRITICAL** |
| Vĩnh Hy | 50% | HIGHLIGHTS, TRANSPORT arrays | **CRITICAL** |
| Gallery | 90% | Gallery items in constants | HIGH |
| Contact | 70% | SITE config, map text | MEDIUM |
| CTA Banner | 100% | (none - reusable) | — |
| Constants | 20% | PROPERTIES, GALLERY_ITEMS, SITE | **CRITICAL** |

---

## Unresolved Questions

1. Should collection card images use CMS category-based fallback gradients if image missing?
2. Should activity/highlight icons be stored as SVG in CMS or use icon library ID?
3. Should SITE config (phone/email/hours) be single CMS document or per-page settings?
4. Should navigation links (NAV_LINKS) be dynamic CMS or stay in constants?
5. Should GALLERY_ITEMS be fetched from separate CMS collection or part of page content?
6. Should form labels (Name, Email, etc.) be CMS-driven for i18n support?
7. Should collection gradients be generated from color scheme or stored per category in CMS?

